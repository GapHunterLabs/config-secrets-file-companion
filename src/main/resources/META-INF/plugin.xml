<!-- Plugin Configuration File. Read more: https://plugins.jetbrains.com/docs/intellij/plugin-configuration-file.html -->
<idea-plugin>
    <id>dev.gaphunter.configsecretsfilecompanion</id>
    <name>Config Secrets File Companion</name>
    <vendor>Gap Hunter Labs</vendor>
    <description><![CDATA[
        <p>Warning on a suspected real secret (AWS access key, GitHub/
        Slack token, JWT, PEM private key, or a high-entropy value
        assigned to a secret-shaped key name) hardcoded directly in a
        config file -- <code>.properties</code>, <code>.env</code> (and
        its variants), or a <code>.yml</code>/<code>.yaml</code> file.
        Config files are a real, common, separate place secrets get
        committed by accident -- a real value pasted into
        <code>application.yml</code> "just for local testing" and never
        removed.</p>
        <p><b>100% static text analysis</b> -- known credential-format
        signatures (AWS/GitHub/Slack/JWT/PEM) plus a variable-name +
        Shannon-entropy heuristic for the generic case, no network
        calls, works offline. Complements source-code secret scanners
        (this catalog's own api-security-companion included), which
        only ever look at Java/Kotlin files, not config files.</p>
        <p>Stated honestly: v0.1 handles single-line
        <code>key=value</code>/<code>key: value</code> pairs only --
        YAML block scalars and multi-line values aren't specially
        handled. The entropy heuristic is a real heuristic, not a
        guarantee -- it can miss a real secret with unusually low
        entropy, or (rarely) flag a long random-looking non-secret
        value.</p>
        <p>Free. No license nags, no telemetry, no network calls.</p>
    ]]></description>

    <depends>com.intellij.modules.platform</depends>

    <extensions defaultExtensionNs="com.intellij">
        <localInspection language=""
            groupName="Config Secrets File Companion"
            displayName="Hardcoded secret in config file"
            enabledByDefault="true"
            level="WARNING"
            implementationClass="dev.gaphunter.configsecretsfilecompanion.inspection.HardcodedConfigSecretInspection"/>
        <notificationGroup id="Config Secrets File Companion" displayType="BALLOON"/>
    </extensions>
</idea-plugin>
